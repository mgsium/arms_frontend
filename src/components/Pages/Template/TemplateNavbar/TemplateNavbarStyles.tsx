import { css } from "emotion";

const Styles = {
    templateNavbarStyles: css`
        background: #15202b;

        & .navbar-brand {
            font-family: Quicksand, sans-serif;
            font-weight: semi-bold;
        }
    `
};

export default Styles